services:
  - type: web
    name: impact-registry
    runtime: python
    repo: https://github.com/NSC-Rick/impact_registry
    branch: main
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run app.py --server.port=$PORT --server.address=0.0.0.0
    
    # Persistent disk for workspace storage
    # Mounts to /var/data/workspaces (matches WORKSPACE_ROOT env var)
    disk:
      name: impact-registry-workspaces
      mountPath: /var/data/workspaces
      sizeGB: 1
    
    # Environment variables
    envVars:
      - key: WORKSPACE_ROOT
        value: /var/data/workspaces
      - key: STREAMLIT_SERVER_HEADLESS
        value: true
      - key: STREAMLIT_SERVER_ENABLE_CORS
        value: false
      - key: STREAMLIT_BROWSER_GATHER_USAGE_STATS
        value: false
